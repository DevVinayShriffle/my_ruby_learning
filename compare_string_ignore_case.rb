# Check if two strings are equal ignoring case:

str = "Admin"
newstr = "admin"

if (str.downcase == newstr.downcase)
    puts "Equal string ingnoring case"
else
    puts "Not equal string"
end
